name: iq_study_ai_mobile

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # state management and http
  flutter_riverpod: ^2.0.0
  dio: ^5.0.0
  go_router: ^7.0.0
  flutter_secure_storage: ^8.0.0
  cached_network_image: ^3.2.0
  hive: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
